<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAG" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Module1"
        tools:targetApi="31">

        <activity
            android:name=".SlimpleTextClientActivity"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".VirtualWaitress"
            android:label="@string/title_activity_virtual_waitress"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Order_Type"></activity>
        <activity android:name=".Starters"></activity>
        <activity android:name=".Veg"></activity>
        <activity android:name=".N_Veg"></activity>
        <activity android:name=".Dessert"></activity>

        <activity
            android:name=".finalize_order"
            android:label="@string/title_activity_finalize_order"></activity>
        <activity
            android:name=".thankyou"
            android:label="@string/title_activity_thankyou"></activity>

    </application>


</manifest>